# Supabase (anon/publishable key only — safe for client-side use, RLS
# scopes what it can actually see)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional. Defaults to http://localhost:3001 if not set.
VITE_API_URL=
